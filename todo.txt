save sequences of inputs as commands / scripts $ via chat

LATER:
Add Touch Screen support
Add Keyboard manager with switchable keyboards that activate the touchscreen


MAYBE:
asynchrounous inputs (multiple buttons at once)


DONE:
Multiple Commands in the same line
"Multiply" Commands with modifier key (+)
holding down keys !hold !release !toggle / <key>:h <key>:r <key>:t
Config Profiles
move
  allowed keys
  key remaps
  to profiles
save/load vars (ints) to/from config folder
implement script execution